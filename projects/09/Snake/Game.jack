class Game {
  field Snake snake;

  constructor Game new() {
    let snake = Snake.new(128, 256);
    return this;
  }

  method void dispose() {
    do snake.dispose();
    do Memory.deAlloc(this);
    return;
  }
}
